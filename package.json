{
  "name": "patrullbyggare",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "check": "node test/check-syntax.js",
    "lint": "eslint .",
    "lint:csv": "node lint_csv.js",
    "test": "node test/run-tests.js",
    "ci": "npm run lint && npm run check && npm test"
  },
  "engines": {
    "node": ">=12"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "globals": "^17.12.0"
  }
}
